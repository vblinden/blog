package partials

templ Link(description string, attrs templ.Attributes) {
	<a { attrs... } rel="noreferrer"
		class="text-blue-600 hover:text-blue-800 dark:text-zinc-200 dark:underline dark:hover:text-white">{ description }</a>
}
